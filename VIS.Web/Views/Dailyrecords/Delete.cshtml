@model VIS.Models.Views.DailyrecordsViewModel

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Datum)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Datum)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Pocet)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Pocet)
        </dd>

        @if (Model.Activity != null)
        {
            <dt>
                @Html.DisplayNameFor(model => model.Activity.Nazev)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Activity.Nazev)
            </dd>
        }
        @if (Model.Meal != null)
        {
            <dt>
                @Html.DisplayNameFor(model => model.Meal.Nazev)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Meal.Nazev)
            </dd>
        }
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
